import { useState, type FormEvent } from "react";
import { useServerFn } from "@tanstack/react-start";
import { CheckCircle2, LoaderCircle } from "lucide-react";
import { submitContact } from "@/lib/submissions.functions";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

export function ContactForm(){const send=useServerFn(submitContact);const[busy,setBusy]=useState(false);const[done,setDone]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=e.currentTarget,fd=new FormData(f);try{await send({data:{name:String(fd.get("name")),business_name:String(fd.get("business_name")),email:String(fd.get("email")),phone:String(fd.get("phone")),message:String(fd.get("message")),website:String(fd.get("website"))}});setDone(true);f.reset()}catch(err){setError(err instanceof Error?err.message:"Please try again.")}finally{setBusy(false)}}if(done)return <div className="py-12 text-center"><CheckCircle2 className="mx-auto size-12 text-accent-foreground"/><h2 className="mt-4 font-serif text-3xl text-primary">Message Received</h2><p className="mt-3 text-muted-foreground">Thank you. Clean Scene will follow up with you soon.</p></div>;return <form onSubmit={submit} className="space-y-5"><input name="website" className="hidden" tabIndex={-1}/>{[["Name *","name","text"],["Business Name","business_name","text"],["Email *","email","email"],["Phone","phone","tel"]].map(([l,n,t])=><label key={n} className="grid gap-2 text-sm font-semibold">{l}<Input name={n} type={t} required={l.includes("*")} maxLength={255}/></label>)}<label className="grid gap-2 text-sm font-semibold">Message *<Textarea name="message" required maxLength={3000} rows={7}/></label>{error&&<p className="text-sm font-semibold text-destructive">{error}</p>}<Button type="submit" size="lg" disabled={busy}>{busy&&<LoaderCircle className="animate-spin"/>}Send Message</Button></form>}